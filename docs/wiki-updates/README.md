# Wiki updates for disc folder output

The GitHub Wiki is a separate repository. These documentation changes were
published directly in wiki commit `48e24b6` and are already live.

`disc-folder-output.patch` is retained as a review snapshot against base wiki
commit `fbfcaf59a7ac3d99e10bfc4a80f43eea28d59523`. Do not apply it again to
the updated wiki.

Future wiki changes must be committed and pushed in the same task as the
related code changes, without waiting for the code PR to merge. See `AGENTS.md`.
